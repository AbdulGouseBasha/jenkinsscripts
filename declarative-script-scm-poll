pipeline {
    agent { label 'agent01' }

    stages {
        stage('Clone Repository') {
            steps {
                git credentialsId: 'abdulgousebasha', branch: 'master', url: 'https://github.com/AbdulGouseBasha/my-app.git/'
            }
        }
        stage('Verify Checkout') {
            steps {
                sh 'ls -la'
            }
        }
        stage('Maven clean') {
            steps {
                sh 'mvn clean'
            }
        }
        stage('Maven build') {
            steps {
                sh 'mvn package'
            }
        }

        stage('Test') {
            steps {
                echo "test stage done"
            }
        }
        stage('Deploy') {
            steps {
                echo "deployement stage done"
            }
        }
    }
}
