pipeline {
    agent { label 'agent01' }

    stages {
        stage('Build') {
            steps {
                echo "build stage done"
            }
        }
        stage('Test') {
            steps {
                echo "test stage done"
            }
        }
        stage('Deploy') {
            steps {
                echo "deployement stage done"
            }
        }
    }
}
